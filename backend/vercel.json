{
  "builds": [
    {
      "src": "api/requirements.txt",
      "use": "@vercel/python",
      "config": {
        "pythonVersion": "3.x"
      }
    }
  ],
  "scripts": {
    "build": "python3 -m pip install -r api/requirements.txt"
  }
}
