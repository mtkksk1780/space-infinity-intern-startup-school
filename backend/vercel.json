{
  "builds": [
    {
      "src": "backend/src/api/app.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ],
  "postbuild": {
    "command": "pip install -r backend/requirements.txt && cd backend/src && prisma generate"
  }
}
